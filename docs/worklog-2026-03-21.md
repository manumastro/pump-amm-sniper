# Worklog 2026-03-21

## Obiettivo del ciclo
- Riportare il bot vicino a uno stato storico stabile/profittevole.
- Ridurre complessita non necessaria (skills/MCP/audit) e chiarire la configurazione runtime.
- Ripristinare una run pulita con report affidabili per worker.

## Cosa e stato fatto
- `AGENTS.md` convertito in versione lazy-load con indice ai documenti e sole regole non negoziabili.
- Fix report per-worker nel daemon: i totali PnL ora vengono calcolati da `report.operations` con campi `pnlSol/pnlPct` (non da eventi non arricchiti).
- Eseguiti reset runtime/log multipli e restart servizi (`pump-sniper.service`, `pump-report.service`) con sequenza corretta: build -> stop -> reset -> start.
- Creato branch di restore vicino allo snapshot storico positivo (`restore-20260318-clean`), con rimozione/rollback di parti non desiderate (skills/MCP/audit).
- Resa `src/app/config.ts` la fonte autorevole tramite gating degli override env (`CONFIG_ENV_OVERRIDES_ENABLED`).
- Aggiornato `.env.example` con `CONFIG_ENV_OVERRIDES_ENABLED=false` come default operativo.
- Raggruppate le configurazioni in domini (`PRE_BUY_CONFIG`, `HOLD_CONFIG`, `CREATOR_RISK_CONFIG`, `PAPER_CONFIG`, `RUNTIME_CONFIG`).
- Riavvio finale completato con servizi attivi e report ripartiti da zero dopo reset.

## Branch e commit rilevanti
- `main`
  - `3711930` docs: slim AGENTS.md for lazy-load guidance
  - `94e43c8` fix: correct per-worker pnl stats and document active A/B/C run
- `restore-20260318-clean`
  - `1e6ed96` rollback config/audit disable
  - `638428a` remove audit management paths
  - `14dbc8c` skills/MCP cleanup
  - `ea764a7` config env override gating
  - `0656501` config grouping by domain

## Note operative
- Dopo la pulizia completa di `logs/`, il confronto runtime 1:1 con report storici locali non e piu possibile se i file non sono in backup esterno.
- `logs/archive/` e stato usato per analisi storica ma non e considerato fonte versionata affidabile nel branch corrente.
